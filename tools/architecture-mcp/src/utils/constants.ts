import * as path from "path";

/**
 * Environment Variable Keys
 */
export const ENV_KEYS = {
  DOCS_DIR: "TRADING_PLATFORM_DOCS_DIR",
  LOG_LEVEL: "TRADING_PLATFORM_LOG_LEVEL",
};

/**
 * Server Information
 */
export const SERVER_INFO = {
  NAME: "trading-platform-architecture-mcp",
  VERSION: "1.0.0",
};

/**
 * Protocol Schemes & URIs
 */
export const PROTOCOLS = {
  DOCS: "trading-docs:",
};

export const MIME_TYPES = {
  MARKDOWN: "text/markdown",
};

/**
 * Default relative directories and fallback paths
 */
export const PATH_DEFAULTS = {
  DOCS_DIR_NAME: "docs",
};
